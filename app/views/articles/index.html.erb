<h1>Your articles</h1>
  <ul>
    <% @articles.each do |article| %>
    <li><%=link_to article.title + ": " + article.content, article_path(article) %><li>

    <% end %>

  </ul>
    <button><%= link_to 'Create new article', new_article_path  %></button>
